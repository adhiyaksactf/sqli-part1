version: "3.7"
services:
  app:
    build:
      context: ./
      dockerfile: Dockerfile
    image: sqli-part1
    container_name: sqli-part1-app
    restart: always
    ports:
      - "80:80"
    depends_on:
      - db

  db:
    image: mysql:5.7
    container_name: sqli-part1-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: sqli
      MYSQL_PASSWORD: sqli
      MYSQL_DATABASE: sqli-login
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql
    ports:
      - "3306:3306"
    volumes:
      - ./db:/docker-entrypoint-initdb.d
